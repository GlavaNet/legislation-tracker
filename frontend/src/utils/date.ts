// Date formatting utilities
export const formatDate = (date: string) => {
  return new Date(date).toLocaleDateString();
};

export const getTimeAgo = (date: string) => {
  // Placeholder for time ago formatting
  return '';
};
